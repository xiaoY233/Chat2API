# Replace YOUR_GITHUB_USER with the owner of the fork that publishes the image.
CHAT2API_IMAGE=ghcr.io/YOUR_GITHUB_USER/chat2api-headless:sha-REPLACE_WITH_COMMIT_SHA

# The service remains bound to the VPS loopback interface.
CHAT2API_HOST_PORT=8080
